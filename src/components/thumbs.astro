---
interface Props {
  thumbs?: string[];
}

const { thumbs = [] } = Astro.props;
---

<div class="thumbs">

  {thumbs.map((image: string) => (
    <img 
      src={image} 
      alt="" 
      loading="lazy" 
    />
  ))}
</div>


